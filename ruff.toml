line-length = 100
target-version = "py312"

[lint]
select = ["E", "F", "W", "I", "UP", "B"]

[lint.isort]
known-first-party = ["common", "data_generator"]

[lint.per-file-ignores]
"tests/**" = ["B011"]
